// 1. Array Literal (Recommended and Most Common)
const fruits = ["Apple", "Banana", "Mango"];

// 2.  Array Constructor (new Array())
const numbers = new Array(10, 20, 30);

// 3. Creating an Array from a String (split() method)
const data = "Red,Green,Blue";
const colors = data.split(",");   // output : ["Red", "Green", "Blue"].

// 4.  Using Array.of()
const numbers1 = Array.of(1, 2, 3);   // output : [1, 2, 3].
